{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0"
  },
  "scripts": {
    "install": "lerna bootstrap",
    "publish": "lerna publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
